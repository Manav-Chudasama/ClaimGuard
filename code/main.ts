/**
 * ClaimGuard — Multi-Modal Evidence Review System
 *
 * Entry point: orchestrates the full claim verification pipeline.
 * Usage: bun run main.ts
 */

import { run } from "./src/index.js";

await run();
